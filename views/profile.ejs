<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>User Profile</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* General Page Styling */
    body {
      background-color: #f5f5f5; /* Subtle light gray background */
      font-family: 'Arial', sans-serif;
      color: #333; /* Neutral text color */
    }

    /* Profile Card */
    .profile-container {
      max-width: 600px;
      margin: 50px auto;
      padding: 20px;
      background-color: #ffffff; /* White background for the card */
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    /* Profile Picture */
    .profile-picture {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      object-fit: cover;
      border: 3px solid #dee2e6;
    }


    /* Username */
    .username {
      font-weight: bold;
      font-size: 1.5rem;
      text-align:20px;
      margin-bottom: 20px; /* Space between username and bio */
    }

    /* Bio Section */
    .bio {
      background: #f8f9fa;
      padding: 10px 15px;
      border-left: 4px solid #6c757d;
      border-radius: 5px;
      font-style: italic;
      font-size: 1.1em;
      margin-bottom: 20px;
    }

    /* Social Media Links */
    .social-icons p {
      display: flex;
      align-items: center;
      margin-bottom: 10px;
    }

    .social-icons img {
      width: 20px;
      height: 20px;
      margin-right: 8px;
    }

    .social-icons a {
      text-decoration: none;
      color: #495057;
      font-weight: 500;
      transition: color 0.3s;
    }

    .social-icons a:hover {
      color: #007bff; /* Blue highlight on hover */
    }

    /* Edit Button */
    .edit-btn {
      position: fixed;
      bottom: 20px;
      right: 20px;
      background-color: #007bff;
      border: none;
      color: white;
      border-radius: 5px;
      padding: 10px 30px; /* Increased width */
      font-size: 1rem;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }
  </style>
</head>
<body>

<div class="container profile-container">
  <!-- Profile Picture -->
  <img src="<%=user.image.url%>" alt="Profile Picture" class="profile-picture">
  <!-- Username -->
   <br>
  <p class="username"><%=user.username%></p>

  <div class="content">
    <!-- Styled Bio Section -->
    <p class="bio"><%=user.bio%></p>

    <!-- User Details -->
    <p><strong>Gender:</strong><%=user.gender%></p>
    <p><strong>Blogs:</strong><%=user.blogs.length%></p>
    <p><strong>Likes:</strong><%=user.likedblogs.length%></p>
    <p><strong>Followers:</strong><%=user.followers.length%></p>
    <p><strong>Following:</strong><%=user.following.length%></p>
  
    <!-- Social Media Links -->
    <div class="social-icons">
      <p><img src="https://img.icons8.com/ios-filled/50/000000/x--v1.png" alt="X">
      <a href="https://x.com/user" target="_blank"><%=user.twitter%></a></p>
      <p><img src="https://img.icons8.com/ios-filled/50/000000/linkedin.png" alt="LinkedIn">
      <a href="https://linkedin.com/in/user" target="_blank"><%=user.linkden%></a></p>
      <p><img src="https://img.icons8.com/ios-filled/50/000000/facebook.png" alt="Facebook">
      <a href="https://facebook.com/user" target="_blank"><%=user.facebook%></a></p>
    </div>
  </div>
</div>

<!-- Edit Button -->
<button class="btn edit-btn" onclick="location.href='/profile/edit'">Edit Profile</button>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>